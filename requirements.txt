# 분석/추천/UI 환경용 (Python 3.10+)
# SC-FEGAN(TF 1.13)은 별도 Colab 환경에서 동작 — notebooks/01_env_check.ipynb 참조

mediapipe>=0.10.0,<=0.10.21
opencv-python>=4.5
numpy>=1.21
pyyaml>=6.0
streamlit>=1.30
reportlab>=4.0
Pillow>=10.0
scikit-image>=0.21      # SSIM 평가 (노트북 11)

# ── 모델 학습 (Phase 7-B Refinement) ──
torch>=2.0
torchvision>=0.15
segmentation_models_pytorch>=0.3
albumentations>=1.3

# ── 데이터 (Colab에서 자동 다운) ──
datasets>=2.14
huggingface_hub>=0.20

# ── 개발/테스트 ──
pytest>=7.0
pytest-cov>=4.0
